server:
  host: localhost
  port: 8442

innerClient:
  host: localhost
  port: 8442


spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/festivos
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update